{
  "meta": {
    "title": "Regex Tester Online Free — Web Tool Hub",
    "description": "Test regular expressions live: highlighted matches, capture groups, replace preview, and a quick-reference cheat sheet — instantly in your browser, no upload."
  },
  "heading": "Regex Tester",
  "tagline": "Test regular expressions with live highlighting, capture groups, and a replace preview — entirely in your browser.",
  "ui": {
    "patternLabel": "Pattern",
    "patternPlaceholder": "e.g. ^[\\w.+-]+@[\\w-]+\\.[a-zA-Z]{2,}$",
    "flagsLabel": "Flags",
    "flagGlobal": "global — find all matches",
    "flagIgnoreCase": "ignore case",
    "flagMultiline": "multiline — ^ and $ match line breaks",
    "flagDotAll": "dot all — . also matches newlines",
    "flagUnicode": "unicode",
    "flagSticky": "sticky",
    "testStringLabel": "Test string",
    "testStringPlaceholder": "Paste or type text to test your pattern against…",
    "invalidPatternError": "Invalid pattern: {{message}}",
    "highlightedHeading": "Highlighted matches",
    "matchesHeading": "Matches",
    "matchCount": "{{count}} match(es)",
    "noMatches": "no matches",
    "matchLabel": "Match {{index}}",
    "fullMatchLabel": "Full match",
    "groupLabel": "Group {{index}}",
    "namedGroupLabel": "Group \"{{name}}\"",
    "indexLabel": "index",
    "replaceHeading": "Replace",
    "replacementLabel": "Replacement (use $1, $2… for groups, $<name> for named groups)",
    "replacementPlaceholder": "Replacement text…",
    "resultLabel": "Result",
    "copy": "Copy",
    "copied": "Copied!",
    "clear": "Clear",
    "cheatSheetHeading": "Quick reference",
    "cheat1": "\\d — any digit (0–9)",
    "cheat2": "\\w — any word character (letter, digit, underscore)",
    "cheat3": "\\s — any whitespace character",
    "cheat4": ". — any character except a line break",
    "cheat5": "^ — start of string (or line, with the multiline flag)",
    "cheat6": "$ — end of string (or line, with the multiline flag)",
    "cheat7": "* — zero or more of the previous token",
    "cheat8": "+ — one or more of the previous token",
    "cheat9": "? — zero or one of the previous token",
    "cheat10": "{n,m} — between n and m repetitions",
    "cheat11": "(...) — capturing group, (?:...) — non-capturing group",
    "cheat12": "\\b — word boundary, | — alternation (OR)"
  },
  "related": "Related tools",
  "article": {
    "heading": "How does testing a regex in the browser actually work?",
    "p1": "A regular expression is a compact pattern language for matching text — everything from \"does this look like an email address\" to \"find every phone number in this document\" to \"strip HTML tags from this string.\" The catch is that regex syntax is notoriously easy to get subtly wrong: a `.` that should be escaped, a greedy `*` that swallows more than intended, a group that isn't capturing what you expect. Writing one blind and only finding out it's wrong once it's already running against production data is a bad way to learn that lesson.",
    "p2": "This tool runs entirely on JavaScript's own built-in `RegExp` engine, executing directly in your browser the moment you type — there's no server involved, so whatever text you're testing against (which might be real user data, log lines, or anything else you'd rather not paste into a third-party service) never leaves your machine. Because it's the exact same engine your JavaScript code will run in production, what you see here — which characters match, how greedy quantifiers behave, whether a group captured anything — is exactly what you'll get at runtime, not an approximation from a different regex flavor.",
    "p3": "Type a pattern and flip on the flags you need: `g` to find every match instead of stopping at the first one, `i` for case-insensitive matching, `m` to make `^` and `$` match the start/end of each line rather than the whole string, `s` to let `.` match newlines too. Every match in your test string is highlighted live, and each one is broken down below into its full matched text, its position, and any capture groups — including named groups like `(?<year>\\d{4})` — so you can immediately see whether the group you meant to extract is actually the one that matched. The Replace section shows exactly what `String.replace()` would produce with your pattern and a replacement string, including group backreferences like `$1` or `$<year>`.",
    "p4": "The quick-reference panel below covers the tokens people forget most often mid-task — the difference between `*` and `+`, what a non-capturing group `(?:...)` is for, how `\\b` marks a word boundary — without needing to leave the page to look them up. Once you've got a pattern that behaves the way you want against realistic test data, it's ready to drop straight into your code. If you need to validate or reformat the surrounding data itself, the JSON Formatter & Validator and Text Diff Checker tools are natural companions."
  }
}
